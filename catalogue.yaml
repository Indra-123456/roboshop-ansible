- name: installing catalogue
  hosts: catalogue
  become: yes
  tasks:
  - name: enable 18 version
    ansible.builtin.shell: dnf module disable nodejs ; dnf module enable nodejs:18 

  - name: install nodejs
    ansible.builtin.package:
      name: nodejs 
      state: present